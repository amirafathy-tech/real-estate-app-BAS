ID: re-custom
_schema-version: "3.1"
version: 1.0.0

modules:
  - name: re-custom
    type: html5
    path: app/re-custom/dist
    build-parameters:
      builder: npm
      build-result: dist
      supported-platforms: []
    parameters:
      disk-quota: 128M
      memory: 128M
    properties:
      CORS:
        - uriPattern: .
          allowedMethods: ["GET", "POST", "PUT", "DELETE"]
          allowedOrigin: ["*"]